{
  "name": "otpsync-backend",
  "version": "1.0.0",
  "description": "OTPSync Convex Backend",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "generate": "convex codegen"
  },
  "dependencies": {
    "convex": "^1.17.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
